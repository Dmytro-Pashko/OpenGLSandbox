package com.dpashko.sandbox.scene.editor

import com.dpashko.sandbox.scene.Controller
import javax.inject.Inject

class EditorController @Inject constructor() : Controller {

    var worldSize = 64f

    override fun init() {
    }

    override fun tick() {
    }

    override fun dispose() {
    }
}